const HOME = "/";

const USER = "/user";
const LOGIN = "/login";
const SIGNUP = "/signUp";
const SENDEMAIL = "/sendEmail";
const IDCHECK = "/idCheck";

const ME = "/me";

const ERROR = "/error";
const routes = {
  HOME,
  USER,
  LOGIN,
  SIGNUP,
  IDCHECK,
  SENDEMAIL,
  ME,
  ERROR,
};
export default routes;
