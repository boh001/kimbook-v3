const CONTENT = "/content";
const UPLOAD = "/upload";

const USER = "/user";
const LOGIN = "/login";
const SIGNUP = "/signUp";
const SENDEMAIL = "/sendEmail";
const IDCHECK = "/idCheck";
const INFO = "/info";

const ME = "/me";

const ERROR = "/error";
const routes = {
  CONTENT,
  UPLOAD,
  USER,
  LOGIN,
  SIGNUP,
  IDCHECK,
  SENDEMAIL,
  INFO,
  ME,
  ERROR,
};
export default routes;
